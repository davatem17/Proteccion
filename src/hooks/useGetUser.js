import React, {useState, useEffect} from 'react';
import axios from 'axios';

const useGetUser = (email, password) => {
    const [user, setUser] = useState({});
    
    useEffect(() => {
        
            
    },[]);
    return user;
}
 
export default useGetUser;